# Eval: compose without explicit path

User: Export this as a Markdown file.

Source title-like material: Solana Wallet Research.

## Required

- Infer a reasonable kebab-case `.md` filename such as `solana-wallet-research.md`.
- Use `docs/` only when that directory already exists and is the established convention.
- Do not invent a nested directory tree.
- Report the chosen path.
- File is created and verifies.

## Prohibited

- Asking repeatedly for a filename when one is obvious.
- Creating `research/exports/final/` style paths.
