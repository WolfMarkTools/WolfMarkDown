# Already Good

Privy is selected for the prototype signing architecture.

## Details

- Uses the required signing model
- Keeps a documented fallback

See `./scripts/format-markdown.mjs` and https://example.com/docs.
